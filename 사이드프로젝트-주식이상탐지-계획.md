# 트랙 B — 개인 사이드 프로젝트: 실시간 시계열 이상탐지 + 풀 MLOps

> 목적: 데이터 수집 → 모델 → 서빙 → 모니터링 → 알람까지 **end-to-end를 혼자 끝까지** 만든 증거.
> 사내(트랙 A)에서 못 만지는 것 담당 — 풀 파이프라인 소유권, 클라우드, 분산 설계 패턴, 파운데이션 모델 실증.
> GitHub 공개 + 데모 가능 = 면접에서 화면 보여줄 수 있는 카드.
> 작성일: 2026-06-24

---

## 0. 전제 / 설계 원칙

| 항목 | 방향 |
|---|---|
| 도메인 | 주식/투자 데이터 (이미 개인적으로 다루는 영역 = 동력 확보) |
| 헤드라인 태스크 | **시계열 이상탐지** (급변·이상 거래·regime shift) → 설비 이상탐지와 개념 직결 |
| 모델 전략 | 통계 baseline + **시계열 파운데이션 모델** — 트랙 A와 달리 여기선 FM이 *이기는* 그림을 보여줌 |
| 스케일 | 실데이터는 작아도 **확장 가능한 설계 패턴**(스트리밍·분산·컨테이너) + 다중 스트림 시뮬레이션으로 throughput 압박 |
| 범위 규율 | 8주 솔로는 빡빡 → **MVP 먼저, 나머지는 stretch**로 명시 |

**중요 — FM이 이기게 하려면:** history 긴 시리즈를 골라야 함(트랙 A가 진 이유의 반대). 자기 과거값에서 신호가 나오는, 길고 연속적인 시계열을 데이터로 선택.

---

## 1. 데이터 소스 선택 (먼저 결정)

| 옵션 | 장점 | 단점 |
|---|---|---|
| **암호화폐 틱** (Binance WebSocket 등) | 무료 · **진짜 고빈도** → 스트리밍 throughput 서사 강력 · 24/7 | 도메인이 주식과 약간 다름 |
| 주식 일/분봉 (yfinance, pykrx, Alpha Vantage) | 익숙 · 친숙한 도메인 | 무료 틱은 어려움 → throughput은 시뮬레이션으로 보완 |

> 권장: **암호화폐 틱으로 스트리밍 백본**을 만들고(진짜 부하), 주식 일봉으로 예측/이상탐지 모델을 붙이는 하이브리드도 가능. 단순하게 가려면 한쪽만 골라도 됨.

---

## 2. 작업 스트림 (파이프라인 레이어별)

### A. 데이터 파이프라인
- [ ] 수집: API/WebSocket → 스트림 소스
- [ ] 스트리밍: **Kafka** (topic 설계, producer/consumer)
- [ ] 저장: **Parquet + 파티셔닝**(날짜/심볼 키) — "노드 늘리면 스케일된다" 구조
- [ ] 배치 오케스트레이션: **Airflow DAG** (정기 수집·집계) — apt-insight 경험 재활용

### B. 모델 (이상탐지)
- [ ] baseline: 잔차 기반 / Z-score / 이동평균 대비 편차
- [ ] **파운데이션 모델**: Chronos / TimesFM zero-shot으로 다음 시점 예측 → **예측오차 큰 지점 = 이상**
- [ ] 평가 설계: 이상탐지는 라벨이 없음 → **합성 이상 주입**(spike/level-shift)으로 정량 평가 + 알려진 과거 이벤트로 정성 검증
- [ ] MLflow로 실험·버전·지표 추적

### C. 서빙
- [ ] **FastAPI** 추론 API (점수/이상여부 반환)
- [ ] **Docker** 컨테이너화
- [ ] (선택) 실시간 스코어링: Kafka consumer가 스트림을 모델에 흘려보냄

### D. 클라우드 배포 + 분산 패턴
- [ ] **GCP/AWS** 배포 (Linux 환경 운영 경험)
- [ ] (stretch) **K8s**에 올려 수평 확장 구조 — replica/HPA
- [ ] (stretch) **PySpark / Spark Structured Streaming**으로 분산 처리 (배치 집계 or 스트림)
- [ ] **스케일 검증**: N개 심볼/센서 스트림 시뮬레이션 → 처리량·지연(latency) 측정·기록

### E. 모니터링 / 알람
- [ ] **Evidently**: 입력 분포 드리프트 리포트
- [ ] **Prometheus + Grafana**: 처리량·지연·이상탐지율 대시보드
- [ ] 임계치 알람 (드리프트 or 이상 급증 시)

### F. 문서화 / 포트폴리오
- [ ] README + **아키텍처 다이어그램**
- [ ] 벤치마크 결과(baseline vs FM) + 스케일 측정치
- [ ] "설비 이상탐지로의 전이 가능성" 한 단락 — 면접 연결고리

---

## 3. 8주 로드맵 (MVP / stretch 구분)

| 주차 | 내용 | MVP | stretch |
|---|---|---|---|
| **Wk1–2** | A. 데이터 파이프라인 | API 수집 + Kafka + Parquet 저장 + Airflow DAG | Spark 집계 |
| **Wk3–4** | B. 모델 | baseline + FM zero-shot + 합성이상 평가 + MLflow | 멀티변량/멀티심볼 |
| **Wk5** | C. 서빙 | FastAPI + Docker | Kafka 실시간 스코어링 |
| **Wk6** | D. 클라우드 배포 | 클라우드 컨테이너 배포 | K8s + HPA |
| **Wk7** | E. 모니터링/알람 | Evidently + Grafana + 알람 1건 | Prometheus 커스텀 메트릭 |
| **Wk8** | D/F. 스케일 검증 + 문서화 | 다중 스트림 throughput 측정 + README/다이어그램 | 부하테스트 자동화 |

**완주 우선순위:** A → B → C → E → 문서화. D의 K8s/Spark는 stretch — 시간 모자라면 "설계는 했고 패턴은 안다" 수준으로 정리해도 OK.

---

## 4. 완료 시 산출물

### 이력서 문장
> 실시간 시계열 이상탐지 시스템을 Kafka·(Spark)·FastAPI·Docker·K8s로 end-to-end 구축. 시계열 파운데이션 모델(Chronos/TimesFM) 기반 예측오차 탐지 + 합성 이상 주입 평가, MLflow 실험관리. Evidently/Grafana 드리프트·처리량 모니터링과 임계치 알람, 다중 스트림 시뮬레이션으로 처리량·지연 검증.

### 직무 역량 커버
- ✅ MLflow / Airflow MLOps — 직접 구축
- ✅ 백엔드 (Python/FastAPI) · 서빙
- ✅ 클라우드 · Linux — 직접 배포·운영
- ✅ 최신 AI — 파운데이션 모델이 *이기는* 실증
- ✅ 드리프트 모니터링 / 알람
- △ 대규모 분산 — 확장 패턴 + 시뮬레이션 (실전 트러블슈팅은 "입사 후 흡수" 프레이밍)

### 면접 카드
- 화면 데모 가능한 라이브 시스템
- "이상탐지 평가를 라벨 없이 어떻게?" → 합성 이상 주입 + 과거 이벤트 검증
- "스케일 어떻게 검증?" → 다중 스트림 시뮬레이션, throughput/latency 수치
- "이게 반도체 설비와 무슨 상관?" → 시계열 이상탐지 방법론 동일, 도메인만 다름

---

## 관련
- 트랙 A(사내 수요예측 고도화)와 분리 진행: 트랙 A = 깊이·신뢰도·도메인, 트랙 B = 폭·자유도·분산.
- apt-insight의 Airflow 경험을 Wk1–2 수집 DAG에 재활용.
