# 사내 수요예측 고도화 — 상세 계획

> 목적: 현재 XGBoost 기반 수요예측 업무를 "모델 돌리는 사람" → **"ML 시스템을 운영하는 사람"**으로 격상.
> MLOps 역량(모델 운영 · 드리프트 모니터링) 강화를 위한 실무 카드 확보.
> 작성일: 2026-06-24

---

## 0. 전제 / 현황

| 항목 | 현재 상태 | 함의 |
|---|---|---|
| 모델 | XGBoost (Prophet 일부) | 피처 기반 tabular 회귀가 본질 → **이게 정답, 유지** |
| 데이터 | 데이터 포인트 한정 · 피처 null 多 | history 짧음 → univariate 파운데이션 모델 부적합 (regime mismatch) |
| 운영 성숙도 | 모델 학습/예측 중심 | **여기를 끌어올리는 게 이 계획의 본체** |

**핵심 방향:** 모델을 바꾸지 않는다. 모델 주변의 *실험관리 → 평가체계 → 모니터링 → 재학습*을 운영 레벨로 올린다.

---

## 1. 작업 스트림

### ① 평가 체계 정립 — 가장 먼저, 가장 중요
- [ ] 시계열 검증: **rolling / expanding window backtest** (random split 금지 — 누수)
- [ ] 지표: MAPE 단독 금지 → **WAPE / bias(과대·과소 경향) / 피크 구간 오차** 병행
- [ ] null 처리 전략 문서화 (XGBoost 네이티브 vs 명시적 대체 비교 실험)
- **산출물:** 백테스트 스크립트 + "현 모델 성능 기준선" 문서
- **주의:** ②③④ 전부의 토대. 평가가 흔들리면 나머지 무의미.

### ② 실험 관리 (MLflow)
- [ ] MLflow 로컬/사내 서버 셋업 (단일 사용자는 SQLite/파일 tracking도 가능)
- [ ] 매 실험에 params + ①지표 + 피처셋 로깅
- [ ] 우수 모델 Model Registry 등록 (Staging/Production stage)
- **산출물:** 실험 이력 대시보드 + 모델 버전 선택 근거 추적 가능 상태
- **주의:** 사내 데이터·아티팩트 저장 위치 보안 정책 확인.

### ③ 드리프트 모니터링 + 알람 — 운영 핵심 항목
- [ ] 데이터 드리프트: 피처 분포 변화(PSI / KS-test) + **null 비율 급증** 추적
- [ ] 모델 드리프트: 실제값 확정 후 rolling MAPE/WAPE 추이 + 임계치 알람
- [ ] 도구: Evidently → 사내 알람 채널(메일/메신저/대시보드) 연동
- **산출물:** 정기 드리프트 리포트 + 임계치 알람 1건 이상 실동작
- **주의:** actual이 며칠~몇 주 뒤 확정되는 구조면 평가에 **시차(lag)** 반영.

### ④ 재학습 파이프라인화 (사내 Airflow)
- [ ] DAG: 데이터 적재 → 학습 → ①백테스트 검증 → 통과 시 ②레지스트리 등록
- [ ] 트리거: 정기 스케줄 + ③드리프트 알람 기반 조건부 재학습
- **산출물:** 동작하는 재학습 DAG
- **주의:** 인프라 의존 높음 → **마지막 순위**, 못 해도 ①②③로 충분.

### (보너스) FM 벤치마크 "why not" 리포트 — 1회성, 선택
- [ ] Chronos zero-shot 1회 → "history 짧고 신호가 피처 기반이라 부적합, XGBoost 우위" 정량 기록
- [ ] target 시리즈에 연속 구간 부족 시 "regime상 적용 불가" 결론도 유효
- 부담되면 트랙 B(개인 사이드)로 넘기고 생략 가능.

---

## 2. 단계별 타임라인

| 단계 | 기간(목표) | 내용 | 완료 기준 |
|---|---|---|---|
| **Phase 1** | ~3주 | ① 평가 체계 + ② MLflow | 백테스트 기준선 + 실험 추적 시작 |
| **Phase 2** | ~3주 | (보너스) FM 벤치마크 리포트 | "왜 XGBoost인가" 문서 1건 (팀 공유) |
| **Phase 3** | ~4주 | ③ 드리프트 모니터링 + 알람 | 정기 리포트 + 알람 1건 실동작 |
| **Phase 4** | 이후 | ④ 재학습 DAG | (인프라 허용 시) 자동 재학습 |

**우선순위: ①② → ③ → ④.** ①②③만으로 이력서·면접 카드 완성.

---

## 3. 완료 시 산출물

### 이력서 문장
> 스마트폰 부품 수요예측 모델(XGBoost)을 MLflow 기반 실험·버전 관리 체계로 운영. 시계열 rolling-window 백테스트와 WAPE·bias 지표로 평가 프레임워크를 정립하고, 예측 드리프트·입력 분포 모니터링 및 임계치 알람을 구축. (Airflow 재학습 파이프라인화)

### 직무 역량 커버
- ✅ MLflow / Airflow MLOps
- ✅ 운영 가능한 ML 모델 (검증·모니터링)
- ✅ 실시간 성능(Accuracy/Drift) 모니터링·알람
- ✅ 도메인 이해 (실데이터·실서비스)
- △ 최신 AI — 보너스 리포트로 일부 (본격적인 건 트랙 B에서)

### 면접 대비 포인트
- "시계열 검증 어떻게?" → rolling-window backtest, 누수 방지
- "드리프트 어떻게 정의?" → 데이터(PSI/null율) vs 모델(lagged MAPE) 구분
- "왜 최신 모델 안 썼나?" → regime mismatch 정량 근거 (강한 답변)

---

## 관련
- 별도 트랙 B(개인 사이드 — 주식 이상탐지 + 풀 MLOps)는 분리 진행. 폭·자유도·분산 설계 담당.
